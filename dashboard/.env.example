# Backend API URL
BACKEND_URL=http://localhost:8000

# NextAuth Configuration (for GitHub OAuth)
# Generate NEXTAUTH_SECRET with: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# GitHub OAuth App Credentials (REQUIRED)
# Create at: https://github.com/settings/developers
# Authorization callback URL: http://localhost:3000/api/auth/callback/github
# Scopes requested: repo, read:user
#
# How it works:
# - Users sign in with GitHub OAuth
# - Their access token is stored securely in NextAuth session (encrypted)
# - Token is passed to backend when creating PRs
# - PRs are created from the user's GitHub account (e.g., @username)
#
# Future roadmap:
# - GitHub App support coming for bot-based PRs (soulcaster[bot])
GITHUB_ID=your_github_oauth_app_client_id
GITHUB_SECRET=your_github_oauth_app_client_secret

# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/soulcaster?schema=public"

# Redis (optional)
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# AWS Credentials (for Fargate integration)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# ECS Configuration (from terraform output)
ECS_CLUSTER_NAME=coding-agent-cluster
ECS_TASK_DEFINITION=coding-agent-task
ECS_SUBNET_IDS=subnet-xxx,subnet-yyy
ECS_SECURITY_GROUP_IDS=sg-xxx
